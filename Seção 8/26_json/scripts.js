// Este script mostra um objeto escrito em formato parecido com JSON.
// JSON e um formato de texto usado para representar dados.
// Em JavaScript, podemos trabalhar com objetos diretamente.

let pessoa = { 
    "nome": "Gabriel",
    "idade": 24
};

// Acessa e exibe a propriedade nome do objeto.
console.log(pessoa.nome);

// Acessa e exibe a propriedade idade do objeto.
console.log(pessoa.idade);
